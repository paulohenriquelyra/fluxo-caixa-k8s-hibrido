apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fluxo-caixa.fullname" . }}-test-connection"
  labels:
    {{- include "fluxo-caixa.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fluxo-caixa.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
