apiVersion: v1
kind: Service
metadata:
  name: {{ include "fluxo-caixa.fullname" . }}
  labels:
    {{- include "fluxo-caixa.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fluxo-caixa.selectorLabels" . | nindent 4 }}
